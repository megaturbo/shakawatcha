<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Movie"></style>

    <style name="Movie.Title">
        <item name="android:textSize">@dimen/movie_title</item>
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">@dimen/movie_title_margin_vertical</item>
        <item name="android:layout_marginBottom">@dimen/movie_title_margin_vertical</item>
        <item name="android:layout_marginLeft">@dimen/movie_title_margin_horizontal</item>
        <item name="android:layout_marginRight">@dimen/movie_title_margin_horizontal</item>
    </style>

    <style name="Movie.Year">
        <item name="android:textSize">@dimen/movie_title</item>
        <item name="android:fontFamily">sans-serif-thin</item>
        <item name="android:textStyle">normal</item>
        <item name="android:layout_marginTop">@dimen/movie_title_margin_vertical</item>
        <item name="android:layout_marginBottom">@dimen/movie_title_margin_vertical</item>
        <item name="android:layout_marginLeft">@dimen/movie_title_margin_horizontal</item>
        <item name="android:layout_marginRight">@dimen/movie_title_margin_horizontal</item>
    </style>

</resources>
